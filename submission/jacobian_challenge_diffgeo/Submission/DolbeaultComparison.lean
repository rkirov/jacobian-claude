import Submission.DolbeaultComparison.DolbeaultComparison
import Submission.DolbeaultComparison.DolbeaultComparisonEquiv
import Submission.DolbeaultComparison.DolbeaultComparisonInverse
import Submission.DolbeaultComparison.DolbeaultComparisonProof
import Submission.DolbeaultComparison.DolbeaultH01
import Submission.DolbeaultComparison.GoodCover
import Submission.DolbeaultComparison.LerayCoverExists
import Submission.DolbeaultComparison.LocalRealization

/-!
# Dolbeault Comparison (`Jacobians/DolbeaultComparison/`)

The PDE-free comparison `Čech H¹(X, 𝒪) ≅ H^{0,1}_∂̄(X)`, local realization of cocycles, Mittag-Leffler gluing, and existence of Leray covers.

**Keystones:** `DolbeaultComparison (the iso)`; `LerayCoverExists`

**Builds on units:** cech-cohomology, dbar-solvability, meromorphic-and-divisors
-/
