# Rethink — the token-efficient path (2026-06-05)

Strategic reset after the overnight session. Reproduce state: `lake build` (full glob green, 8467 jobs);
`#print axioms` on any decl. Companion to `docs/architecture_map.md`, `docs/serre_17_build_plan.md`.

## Where we actually are (verified, banked)

- **W2 / `deg_div` (residue theorem) — CLOSED**, axiom-clean. 5 challenge-critical walls → **4**.
- **Serre leaves re-pointed onto Forster §17** (`SerreDualityPairing`): `arithmeticGenus_eq_genus` +
  `serre_h1_eq` → the single node `exists_serreDualityData`; `hodge_symmetry` DEAD.
- **Finiteness `exists_cechModel` — structural half PROVEN, reduced to 2 named obligations**
  (`ChartCoverContinuousLeray` disk-acyclicity + the comparison `cechH1 𝔘 D ≃ supH1`). The δ-complex/δ²=0/
  finiteness-given-Leray are all axiom-clean.
- **`exists_skyscraperLES` reduced** (`skyscraperLES_of_chartDisk` assembly proven) + a soundness finding
  (below).

## The ONE structural insight that should drive everything

**Finiteness (`exists_cechModel`) is the keystone — the whole forward-headline tower bottoms out there.**
Verified this session:
- cohomological RR (χ) needs H¹ **and** H⁰ finiteness (the skyscraper agent confirmed `H⁰(𝒪_{D+P})`
  finiteness `l(D+P)<∞` is *genuinely absent* from the repo — it is part of the finiteness theorem).
- the §17 Serre pairing needs RR dim-estimates **and** the residue theorem's nonconstant meromorphic
  function — both come from RR, i.e. from finiteness.
- `ω₀`-existence (the old "make-or-break unknown") = the cohomological-RR inequality ⇒ also finiteness.

So **`exists_serreDualityData`, `exists_skyscraperLES`'s finiteness gap, and the residue/§17 pairing are
ALL downstream of finiteness.** Finiteness is the single chokepoint.

## The second recurring theme: COVER-INDEPENDENCE

Two independent walls reduce to the *same* missing piece — that Čech H¹ on a Leray cover is independent
of the cover (refinement-invariance):
- finiteness obligation (2): the comparison `cechH1 𝔘 D ≃ supH1` = STEP B (`cechH1 𝔘 ≅ cechH1 chartCover`).
- skyscraper soundness finding: `exists_skyscraperLES 𝔘 hL D P` fixes the cover up front, but a fixed cover
  can't carry the per-point chart-disk geometry (singleton-star / `D`-support) for *every* `(D,P)`. It needs
  either a multi-set local realization OR transport of the **full** 6-term LES across a refinement
  (`LerayCoverIndependence.refineH1_equiv` exists for H¹ alone — not the LES).

⇒ **Build cover-independence ONCE** (refinement-invariance of the Čech objects for Leray covers) and it
discharges finiteness-obligation-(2) AND unblocks the fixed-cover skyscraper. High shared leverage.

## The token-efficient plan (priority order)

**TIER 1 — the keystone (do this, nothing else, until it lands):** `exists_cechModel`.
1a. **Disk-acyclicity / ∂̄-globalization** (`ChartCoverContinuousLeray`): solve ∂̄ per chart-disk (PROVEN
   `DbarDiskCohomology.dbar_solvable_ball`), glue with a partition of unity, show the Čech difference is a
   holomorphic cocycle ⇒ `HasGluedDbarDatum`. Forster §13–14. The genuine analytic wall; best-scaffolded.
   *(An agent is attempting exactly this; its result calibrates how far this is.)*
1b. **Cover-independence** (the comparison / STEP B) — SHARED with skyscraper; build refinement-invariance
   of Čech H¹ for Leray covers once.
   Closing 1a+1b ⇒ finiteness ⇒ cohomological RR ⇒ (with §17) the forward headline's RR spine.

**TIER 2 — Serre §17 pairing** (`exists_serreDualityData`), unblocked by Tier 1: the 1-form residue theorem
`∑Res(α)=0` (Miranda §VIII.3 trace-to-ℙ¹, reusing the W2 fibre apparatus + the proven ℙ¹ residue theorem +
the general higher-order-pole Lemma 3.2), the global `Res`, the pairing, 17.6 injectivity (the in-repo
`exists_formFnResidue_eq_one` witness), 17.9 surjectivity (the proven `serre_surjectivity_dim_core`).

**TIER 3 — the independent walls (separate tracks, lower ROI, no Mathlib scaffolding):** `#3 abelJacobi`
(gated on RR), `#7 exists_cutSurface` (surface topology / Radó — deferred behind RR per the audit), `#1b
HasHolomorphicPrimitives` (manifold de Rham). Do NOT spread thin tokens here while the keystone is open.

**Cleanups (when convenient, cheap):** delete the 2 DEAD sorries — `DegDivResidue.exists_properMapDegree`
(+ `degDiv_eq_zero`, superseded by the proven route) and `HodgeSymmetry.hodge_symmetry` (superseded by §17;
nothing imports the file). Both trigger a rebuild, so batch them.

## Honest scope

~9 real walls remain, the bulk genuinely-hard greenfield analysis (∂̄-globalization, the §17 pairing/1-form
residue theorem, Abel, surface topology, de Rham). This is a multi-session/multi-month effort; there is NO
single quick finish. But the structure is now sharp: **finiteness is the chokepoint** — concentrate there,
then §17, before touching the independent walls. The sub-agent + kernel-`#print axioms` workflow is
sound and reusable (every claimed close was independently axiom-checked this session).
